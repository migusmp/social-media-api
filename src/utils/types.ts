export type ErrorsRegister = {
    name?: string,
    nick?: string,
    email?: string,
    password?: string
}

export type RegisterInfo = {
    name: string,
    nick: string,
    email: string,
    password: string
}